
import { BaseIntegrationAdapter } from '@orvium/integration-sdk/adapters';

/**
 * Re-exporting BaseIntegrationAdapter from the SDK workspace package
 * to maintain backward compatibility for any existing local imports.
 */
export default BaseIntegrationAdapter;
