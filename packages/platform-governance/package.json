{
  "name": "platform-governance",
  "version": "1.0.0",
  "description": "Platform governance, rate limiting, and abuse detection engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "node tools/generate-llms.js || true && vite build",
    "dev": "vite --host :: --port 3000",
    "preview": "vite preview --host :: --port 3000",
    "lint": "eslint . --quiet"
  },
  "author": "Platform",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}