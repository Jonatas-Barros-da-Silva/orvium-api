{
  "name": "@orvium/integration-sdk",
  "version": "1.0.0",
  "description": "ORVIUM Integration SDK for building integrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "node tools/generate-llms.js || true && vite build",
    "dev": "vite --host :: --port 3000",
    "lint": "eslint . --quiet",
    "preview": "vite preview --host :: --port 3000"
  },
  "keywords": [
    "sdk",
    "integration",
    "adapter"
  ],
  "author": "ORVIUM",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}